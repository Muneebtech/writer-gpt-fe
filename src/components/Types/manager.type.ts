export type ManagerType = {
  status: boolean;
  email: string;
  firstName: string;
  id: string;
  lastName: string;
  photoPath: string;
  role: string;
};
export type AddManagerType = {
  email: string;
  id: string | any
};
